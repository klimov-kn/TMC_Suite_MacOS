// Заглушка <windows.h> для сборки под Linux.
//
// Исходники TMC подключают этот заголовок ради базовых типов (BOOL, DWORD,
// HANDLE), работы со временем файла (CreateFile/GetFileTime/CompareFileTime)
// и запуска процессов. Всё перечисленное есть в слое совместимости, поэтому
// перенаправляем сюда — настоящего win32-API за этим не стоит.
//
// Если когда-нибудь обнаружится реальный вызов win32-API, которого в слое нет,
// расширять заглушку наугад нельзя: нужно разобраться, что именно требуется.
#pragma once
#include "tmc_mfc_doc.h"
