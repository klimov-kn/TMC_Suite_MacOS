// windowplacement.h — положение и размер окна между запусками.
//
// В Windows-версии каркас MFC запоминал положение главного окна и восстанавливал
// его при следующем запуске. На Linux этого нет, и окно каждый раз появляется
// там, где решит система — со стороны выглядит как «окна открываются в случайных
// местах».
//
// Здесь то же поведение, что было в Windows: при первом запуске окно ставится по
// центру экрана, дальше — туда, где его оставили. Хранится в тех же настройках
// программы (`~/.config/TMC_Suite/<программа>.conf`), что и остальные параметры.

#pragma once

#include <QString>

QT_BEGIN_NAMESPACE
class QWidget;
QT_END_NAMESPACE

namespace tmcui {

/// Восстановить положение окна и следить за его сохранением при закрытии.
/// `key` — имя окна в настройках (обычно «MainWindow»).
void keepPlacement(QWidget *window, const QString &key = QStringLiteral("MainWindow"));

/// Поставить окно по центру экрана (используется, когда сохранённого места нет).
void centerOnScreen(QWidget *window);

} // namespace tmcui
