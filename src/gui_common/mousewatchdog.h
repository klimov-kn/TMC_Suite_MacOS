// mousewatchdog.h — защита от «залипшего» захвата мыши.
//
// Дочернее окно многодокументного режима (QMdiSubWindow) само захватывает мышь,
// когда начинает перетаскивание или изменение размера. Если отпускание кнопки до
// него не дошло — например, событие обработал вид документа, — захват остаётся.
// После этого ВСЕ щелчки уходят захватившему окну: панель инструментов и меню
// перестают отвечать, программа выглядит зависшей.
//
// Здесь сторож: раз в полсекунды проверяет, что захват есть, а кнопки мыши уже
// отпущены, и в этом случае снимает захват. Для работы программы это незаметно,
// а зависание становится невозможным.

#pragma once

#include <QtGlobal>

QT_BEGIN_NAMESPACE
class QObject;
QT_END_NAMESPACE

namespace tmcui {

/// Включить сторожа захвата мыши. Живёт вместе с переданным объектом.
void installMouseGrabWatchdog(QObject *owner);

} // namespace tmcui
