// appfonts.h — шрифты, вложенные в поставку программы.
//
// Файлы задания хранят имена шрифтов Windows: Times New Roman, Arial,
// Courier New. Подписи на графиках должны занимать столько же места, сколько на
// Windows, иначе они наезжают друг на друга, поэтому вместо них подставляются
// метрически совместимые Liberation Serif / Sans / Mono (см. substituteFamilies
// в src/compat/mfc_gdi.cpp). На Linux эти шрифты обычно есть в системе, на
// macOS их нет — значит, они кладутся внутрь пакета программы и подключаются
// при запуске.
//
// Решение D10, риск Р-2.

#pragma once

namespace appfonts {

/// Подключить шрифты Liberation из ресурсов программы.
///
/// Каталог ищется в таком порядке (берётся первый существующий):
///   1. каталог из переменной окружения `TMC_FONTS_DIR` (для отладки);
///   2. `<Программа>.app/Contents/Resources/fonts` — пакет macOS;
///   3. `fonts` рядом с исполняемым файлом — проверочная сборка на Linux;
///   4. `../share/tmc_suite/fonts` — установка по правилам Linux.
///
/// Если все три семейства (Liberation Serif, Sans, Mono) уже есть в системе,
/// ничего не загружается — незачем держать в памяти вторую копию.
///
/// Вызов безопасен всегда: нет каталога, нет файлов, файл испорчен — функция
/// молча делает меньше, но не завершает программу. Повторные вызовы ничего не
/// делают.
///
/// \return сколько файлов шрифтов удалось подключить (0 — ни одного либо
///         подключение не потребовалось).
int load();

} // namespace appfonts
